providers = ["python"]

[variables]
PYTHONUNBUFFERED = "1"

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "python -m pip install -r ai/requirements.txt"
]

[start]
cmd = "uvicorn ai.server:app --host 0.0.0.0 --port ${PORT}"
